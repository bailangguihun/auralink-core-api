package com.auralink.workflow;

/** Stable public input and output modalities for workflow schema version 1. */
public enum WorkflowModality {
    TEXT_DESCRIPTION,
    POEM,
    IMAGE,
    PAINTING,
    AUDIO,
    VIDEO
}
